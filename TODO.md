[ ] 模型的思考内容需要用浅色的字体显示出来，但是不需要记录到session中。

[ ] 工具调用 的panel，在控制台输出的时候需要渲染工具的名称。

[ ] 命令调用的时候不要输出”command=“字样。文件读取的时候不要输出path=，直接输出文件路径就好了。

[ ] reviewer的输出报告中已经包含了下一轮反馈，同时控制台又重复输出了需要修复的panel。删除后面的需要输出的panel设计。

[ ] 每一轮Reviewer给出反馈意见，需要Coder再次修改代码的时候，其Coder的上下文不再包含上一轮中的思考内容、工具调用的内容。

[ ] ClAUDE.md中的约束和部分用户偏好需要硬编码到代码中，固定插入每一轮的Coder的上下文管理中。（没想好这项设计，先搁置）

[ ] 大模型调用工具出错的时候需要有兜底。调用大模型的时候出错也需要有兜底，不要中断循环。

[ ] 新增\\btw旁路设计。

[ ] 新增飞书机器人与云文档能力。

[ ] 新增提问对话框，让大模型对需求进行澄清 

